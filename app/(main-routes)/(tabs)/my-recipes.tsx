import { View, Text } from 'react-native';

const MyRecipes = () => {
  return (
    <View style={{ justifyContent: 'center', alignItems: 'center', flex: 1 }}>
      <Text>Tab MyRecipes</Text>
    </View>
  );
};

export default MyRecipes;
